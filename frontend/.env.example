# Copy to .env.local to override.
# Left empty in development so requests go through the Vite proxy at /api.
# In production set this to the deployed API origin, e.g. https://api.manero.in
VITE_API_BASE_URL=
